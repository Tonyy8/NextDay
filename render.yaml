services:
  - type: web
    name: nextday-app
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: "pip install -r requirements.txt && pip install -e . && python manage.py collectstatic --noinput && python manage.py migrate"
    # Do NOT use the Render default "gunicorn your_application.wsgi" — this app lives under backend/config/
    startCommand: "gunicorn config.wsgi:application --chdir backend"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.6"
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "True"
      - key: MOCK_MODE
        value: "True"
      - key: DB_ENGINE
        value: "sqlite"
